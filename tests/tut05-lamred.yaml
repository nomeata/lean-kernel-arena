description: |
  Lambda reduction: Tests function application and beta reduction.
  
  Defines `tut05f : Type → Type → Type := fun x y => x` and then `tut05thm : tut05f Prop (Prop → Prop) = Prop`
  which requires the checker to reduce `tut05f Prop (Prop → Prop)` to `Prop`.
leanfile: tests/tutorial.lean
export-decls: ["tut05f", "tut05thm"]
outcome: accept