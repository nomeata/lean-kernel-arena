description: |
  Dependent type: `tut03 : Prop := ∀ (p: Prop), p`
  
  Tests dependent product types (forall) with bound variables.
leanfile: tests/tutorial.lean
export-decls: ["tut03"]
outcome: accept